import React from 'react';

// This component is intentionally disabled to prevent UI blocking.
// Video generation progress is now shown inline in the ResultsGallery.
export const VideoGenerationOverlay: React.FC<any> = () => {
    return null;
};
